<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quintonvr.tjoptjop">

    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.SyncActivity"></activity>
        <activity android:name=".activities.WellnessCheckActivity" />
        <activity android:name=".activities.DirectionActivity" />
        <activity android:name=".activities.PassGenerationActivity" />
        <activity android:name=".activities.ManualEntryActivity" />
        <activity
            android:name=".activities.TempCaptureSettingsActivity"
            android:label="@string/title_activity_temp_capture_settings" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.IDtypeActivity" />
        <activity android:name=".activities.QuestionsActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MaskActivity" />
        <activity android:name=".activities.IdentificationActivity" />
        <activity android:name=".activities.QrScannerActivity" />
        <activity android:name=".activities.MainActivity" /> <!-- <activity android:name=".activities.CameraActivity"> -->
        <activity android:name=".activities.TempScanningActivity" />
    </application>

</manifest>